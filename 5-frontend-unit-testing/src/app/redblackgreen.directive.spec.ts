import {RedblackgreenDirective} from './redblackgreen.directive';

describe('RedblackgreenDirective', () => {
  it('should create an instance', () => {
    // @ts-ignore
    const directive = new RedblackgreenDirective();
    expect(directive).toBeTruthy();
  });
});
